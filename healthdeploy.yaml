apiVersion: apps/v1
kind: Deployment
metadata:
  name: health6-eta-deploy
  labels:
    app: health6-eta-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: health6-eta-app
  template:
    metadata:
      labels:
        app: health6-eta-app
    spec:
      containers:
      - name: health6-eta-container
        image: shaik0103/health6-eta-app
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: health6-eta-np-service
  labels:
    app: health6-eta-app
spec:
  selector:
    app: health6-eta-app

  type: NodePort
  ports:
  - nodePort: 31021 
    port: 8082
    targetPort: 8082
